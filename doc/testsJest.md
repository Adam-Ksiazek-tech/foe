#Testy JEST

##Instalacja zależności

docker exec -it foe npm i --save-dev jest @testing-library/react @testing-library/jest-dom ts-jest jest-environment-jsdom


##Uruchomienie

docker exec -it foe npm test parseInvestmentAmount

